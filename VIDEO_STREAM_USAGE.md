# 传送带物体追踪 - 视频流功能使用说明

## 功能概述
系统现已支持完整的实时视频流功能，包括：
- 实时摄像头画面显示
- 带检测框的视频流
- WebSocket 实时推送
- HTTP 接口轮询
- 自动错误恢复

## 使用方式

### 1. 启动系统
```bash
# 启动后端服务
python backend/app.py

# 启动前端开发服务器
cd frontend && npm run dev
```

### 2. 前端界面操作
1. 打开前端界面 (http://localhost:5173)
2. 点击"开始追踪"按钮
3. 系统会自动显示实时视频流

### 3. 视频流模式
- **实时视频模式**：显示真实摄像头画面
- **模拟模式**：当视频流不可用时显示模拟动画
- **测试模式**：点击"测试视频流"按钮手动测试

### 4. 调试功能
- **事件日志**：显示所有追踪事件和视频流状态
- **测试按钮**：手动获取当前视频帧进行测试
- **状态显示**：实时显示FPS、检测数等信息

## 技术实现

### 后端 (backend/)
- **视频帧编码**：JPEG格式，Base64编码
- **WebSocket推送**：每3帧发送一次，减少带宽
- **HTTP接口**：`/api/tracking/frame` 支持原始帧和检测帧

### 前端 (frontend/)
- **实时显示**：WebSocket接收视频帧并实时显示
- **错误处理**：视频流失败时自动切换到模拟模式
- **性能优化**：帧率控制和图像压缩

## 常见问题

### Q: 为什么显示的是模拟小球而不是真实视频？
A: 可能原因：
1. 摄像头未正确连接
2. 视频流事件未正确接收
3. 检查事件日志中的错误信息

### Q: 视频流卡顿或延迟？
A: 解决方案：
1. 降低视频质量（调整frame_quality参数）
2. 增加帧间隔（当前每3帧发送一次）
3. 检查网络连接

### Q: 如何验证视频流是否工作？
A: 使用方法：
1. 点击"测试视频流"按钮
2. 查看事件日志中的视频帧接收记录
3. 使用浏览器开发者工具检查WebSocket连接

## API 接口

### 获取视频帧
```javascript
// 获取当前视频帧
const frame = await getCurrentFrame('raw'); // 或 'detections'

// 监听实时视频流
onVideoFrame((data) => {
  const videoFrame = data.frame; // base64编码的图像
});
```

### WebSocket 事件
- `video_frame`: 视频帧数据
- `frame_processed`: 帧处理完成
- `object_detected`: 物体检测
- `object_lost`: 物体丢失

## 性能指标
- **帧率**: 6-8 FPS（取决于硬件）
- **延迟**: < 1秒
- **图像质量**: 可调节（默认30% JPEG质量）
- **网络带宽**: 优化传输，每3帧发送一次

## 故障排除

1. **检查摄像头连接**：
   ```bash
   python backend/test_tracking.py
   ```

2. **测试视频流接口**：
   ```bash
   curl http://localhost:5000/api/tracking/frame?type=raw
   ```

3. **查看系统日志**：
   - 后端控制台输出
   - 前端事件日志
   - 浏览器开发者工具

## 注意事项
- 确保摄像头权限已授予
- 检查防火墙设置
- 验证WebSocket连接正常
- 监控内存使用情况（视频流可能占用较多内存）

现在系统已经完全支持实时视频流功能，用户可以清楚地看到摄像头画面和物体检测效果！